uses Generator
uses UnitTest
uses FunctionData
uses FeatureBuilder

interface CodeSearch {

	void search(char buildFolder[], char buildInterface[], FunctionData functionList[], Generator generator, UnitTest ut, FeatureBuilder featureBuilder)
	
	}
